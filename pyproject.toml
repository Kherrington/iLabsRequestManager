[project]
name = "ilabs-request-manager"
version = "0.1.0"
description = "iLab Service Request Manager"
requires-python = ">=3.10"
dependencies = [
    "requests>=2.28",
    "openpyxl>=3.0",
]

[project.scripts]
ilabs = "app:main"
